using UnityEngine;

namespace Game.Operation
{
    public class OperationFactory
    {
        // TODO: Position = lane?
        public void SpawnOperation(Vector2 position)
        {
            var operation = new Operation();
            // operation.Spawn(position);
            
        }
    }
}